/**
 * Cyber wooden fish (muyu) plugin, node half.
 *
 * Deliberately empty: the widget is a browser-only surface. The node half
 * exists only so the plugin appears in the Loader tree as a first-class row.
 */

/** Host plugin body — nothing to mount on the host side. */
export function apply(): void {}
